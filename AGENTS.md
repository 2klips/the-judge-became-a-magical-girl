# Codex 작업 규칙

## 1. 시작 전

- 모든 작업에서 이 파일과 [문서 지도](docs/dev/PROJECT_MAP.md)를 먼저 읽는다.
- 마일스톤 구현은 [마일스톤 계약](docs/dev/MILESTONES.md)의 해당 절만 범위로 삼는다.
- 시나리오·캐릭터 작업은 [완성대본 v2](docs/심사역은_마법소녀가_되었다_완성대본_v2.md)와 [별첨1](docs/별첨1_스토리_작가_가이드라인.md), 에셋 작업은 [별첨2](docs/별첨2_에셋_제작_가이드라인.md)를 추가로 읽는다.
- 제품 충돌은 최신 사용자 결정 → 메인 기획서 → 스토리·캐릭터 영역의 완성대본 v2 → 해당 별첨 → 승인 ADR → 구현 문서 → 코드 순서로 해결한다.
- `[확정]`, `[제안]`, `[미결정]`을 구분한다. 제안을 확정사항처럼 구현하지 않는다.

## 2. 불변 계약

- A안: LLM 자유 대화 + 스탯 수렴.
- STT 우선. 클릭·로컬 폴백만으로 엔딩까지 완주 가능해야 한다.
- 변신 주문 게이트와 3페이즈 언령 배틀을 유지한다.
- TypeScript + Vite + Vanilla DOM/CSS, zod 런타임 검증을 사용한다.
- LLM 비밀은 Cloudflare Workers에만 둔다. 정적 클라이언트에 키를 넣지 않는다.
- GitHub Pages 배포와 M1→M6 순서를 유지한다.
- MVP·컷 우선순위, 시나리오 JSON·에셋 파일명 계약을 임의 변경하지 않는다.

## 3. 아키텍처·코딩

- 단일 `GameState`와 직렬 FSM을 사용한다. 전역 이벤트 버스·UI 프레임워크를 추가하지 않는다.
- 상태 변경은 검증된 판정 결과를 중앙 함수로 적용한다. LLM 출력을 직접 상태에 대입하지 않는다.
- 도메인 로직은 DOM, Web Speech API, 네트워크, `localStorage`에서 분리한다.
- 분기 조건은 허용 문법만 파싱한다. `eval`/`new Function` 금지.
- 외부 응답·시나리오·복구 스냅샷은 경계에서 zod로 검증한다.
- 새 의존성·스키마 필드·에셋 ID·플래그 추가는 근거와 영향을 [결정 기록](docs/dev/DECISIONS.md)에 남긴다.
- 변경은 활성 마일스톤 구현·테스트·필수 계약 문서로 제한한다.
- 기존 세 기준 문서는 사용자가 기획 변경을 명시하지 않는 한 수정하지 않는다.
- 관련 없는 리팩터링, 다음 마일스톤 선행 구현, 기준 문서 재작성 금지.

## 4. 검증·완료

- 구현 후 `npm run check`, `npm test`, `npm run build`를 실행한다. 스크립트가 아직 없는 pre-M1 상태만 `N/A` 허용.
- 해당 마일스톤의 자동 검증과 수동 플레이 검증을 모두 통과해야 완료다.
- 각 마일스톤 종료물은 실행 가능해야 한다. 실패 경로에서도 클릭·로컬 완주성을 깨지 않는다.
- 시나리오 참조 무결성, 에셋 논리 ID/파일명, 비밀정보 노출을 확인한다.
- 테스트를 건너뛰거나 실패를 숨기지 않는다. 실행하지 못했으면 이유와 남은 위험을 보고한다.

## 5. 보안·데이터

- 실제 API 키, 토큰, 계정값, 예비 키를 코드·문서·로그·커밋에 기록하지 않는다.
- 로컬 비밀 파일은 Git 제외. 예시 파일에는 자리표시자만 둔다.
- Git 작업 전후 `git status --short`와 대상 diff를 확인한다. 관련 없는 파일을 자동 staging하지 않는다.
- 기존 remote를 임의 교체하지 않고 `push --force`, 기록 재작성, 저장소 삭제를 수행하지 않는다.
- M1 최초 게시는 검증된 M1만 초기 commit으로 `main`에 push한다. GitHub Pages 배포는 M6 전까지 수행하지 않는다.
- Origin은 배포 Origin과 명시적 로컬 개발 Origin만 허용한다.
- 작가 소유 JSON 필드를 임의 해석하거나 자동 수정하지 않는다. 오류 위치와 해결법을 한국어로 보고한다.
- 에셋 파일명 변경 시 시나리오 참조와 [에셋 매니페스트](docs/dev/ASSET_MANIFEST.md)를 같은 변경에서 갱신한다.

## 6. 최종 보고

다음 순서로 짧게 보고한다.

1. 결과
2. 변경 파일
3. 검증 명령과 결과
4. 수동 플레이 결과
5. 확정사항과 별도인 제안·가정
6. 남은 위험·다음 단계 진입 가능 여부
